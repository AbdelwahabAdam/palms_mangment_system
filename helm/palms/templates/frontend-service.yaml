{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "palms.frontend.name" . }}
  labels:
    {{- include "palms.componentLabels" (dict "ctx" . "component" "frontend") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "palms.componentSelectorLabels" (dict "ctx" . "component" "frontend") | nindent 4 }}
{{- end }}
